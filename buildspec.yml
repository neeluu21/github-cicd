version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"

  pre_build:
    commands:
      - echo "Pre-build phase"

  build:
    commands:
      - echo "Build phase"
      - echo "Building website..."

  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
